# Build artifacts
node_modules
.next
dist
coverage

# Git and docker meta
.git
.gitignore
.dockerignore
Dockerfile
docker-compose.yml

# Local env (secrets — never bake into the image)
.env
.env.local
.env.*.local

# OS / IDE
.DS_Store
.idea
.vscode

# Project documentation and agent specs are not needed in the runtime image
.claude
docs
WRITEUP.md
README.md

# Tests don't ship in the production image
**/__tests__
**/*.test.ts
**/*.test.tsx
